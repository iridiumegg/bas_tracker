# CLAUDE.md — Project Instructions

## Status Updates

When asked to update the status message:
1. Edit `src/status.js` — update `message` and set `date` to today's date
2. Commit the change
3. Push directly to `main` (this triggers the GitHub Pages deploy workflow)

The live site deploys from `main` only. Changes on feature branches will not appear on the site.
